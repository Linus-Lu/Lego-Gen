# LEGOGen

Project for generating LEGO building instructions from images or text prompts.

## Setup

1. Backend:
   ```bash
   cd backend
   pip install -r ../requirements.txt
   uvicorn app:app --reload
   ```

2. Frontend:
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

